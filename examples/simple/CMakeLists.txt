project (simple)
include_directories(${CMAKE_BINARY_DIR}/include)
link_directories(${CMAKE_BINARY_DIR})
add_executable(simple simple.c)
target_link_libraries(simple openhmd-shared)
install(TARGETS simple DESTINATION bin)